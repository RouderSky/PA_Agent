# 2026-09-22

## prompt_assembler 增量修复全量测试验证
- 环境：系统 Python 3.13.5（bash 里 `python` 指向 WorkBuddy 托管 3.13.12，无项目依赖，跑测试要用 `/c/Program Files/Python313/python.exe`）；补装了 `pytest-qt`（hypothesis/PyQt6 之前已装）；GUI 测试用 `QT_QPA_PLATFORM=offscreen`。
- 全量结果：约 771 个用例，48 failed + 28 errors（GUI setup ERROR），`test_prompt_assembler.py` 全部通过。
- 基线对比方法：修复已提交为 HEAD b81cf2d "1.38"，用 `git worktree add D:/tmp/PA_Agent_baseline_check HEAD~1` 检出基线，跑同一批失败用例 → 48+28 全部在基线同样失败，无一新增。结论：修复无回归，所有失败均为存量问题。
- 预存失败分布：e2e smoke 4、integration 8（akshare_live/next_bar_prediction/switch_mid_analysis/two_stage_no_order）、property 7（json_validator_categories/logs_key/snapshot_bijection/stage2_no_order_invariant）、unit 29（deepseek_client、free_chat_*、decision_continuity、chart_skip_redraw、json_validator、market_defaults、order_*、price_tick、provider_override、pushplus_notifier、qclaw_agent_route、validation_lenient_fixes）+ GUI ERROR 28（chart_fit_view/chart_widget_no_lines/debug_widget/token_indicator，offscreen 下 setup 失败）。
- 已知环境坑：Edit/Write 曾受限，现已恢复；临时文件可放 `.workbuddy/` 下。
